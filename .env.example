# MongoDB 连接配置
# 选择以下其中一种连接方式：

# Docker 方式（推荐）
MONGODB_URI=mongodb://app_user:app_password@localhost:27017/agent_builder

# 本地安装方式
# MONGODB_URI=mongodb://localhost:27017/agent_builder

# 远程 MongoDB 连接（如 MongoDB Atlas）
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/agent_builder

# 应用配置
PORT=3000
NODE_ENV=development

# JWT 配置
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# API 密钥（如果需要）
# OPENAI_API_KEY=your_openai_api_key_here
# DOUBAO_API_KEY=your_doubao_api_key_here